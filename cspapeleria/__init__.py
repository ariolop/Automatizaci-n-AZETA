# Paquete cspapeleria (CS Papelería / Liderpapel).
# Convertido en paquete para evitar la colisión de nombres de módulo
# (config, db, lote, monitor) con la app raíz AZETA al ejecutarse en un
# único proceso (hub unificado).
